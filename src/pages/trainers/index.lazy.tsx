import { lazy } from 'react';

// Trainer Detail (nuova pagina completa)
export const TrainerDetailLazy = lazy(() => import('./TrainerDetail'));

// Trainer Edit
export const TrainerEditLazy = lazy(() => import('./TrainerEdit'));
